# Usage
```
usage: main.py [-h] [--debug] -i INPUT [-o OUTPUT] [--version]

optional arguments:
  -h, --help            show this help message and exit
  --debug               debug output
  -i INPUT, --input INPUT
                        input
  -o OUTPUT, --output OUTPUT
                        path to output directory
  --version             show program's version number and exit
```
